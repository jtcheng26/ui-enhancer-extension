<div class="w-full max-w-3xl rounded-3xl border border-slate-200 bg-white/95 p-6 shadow-sm backdrop-blur">
  <div class="flex items-start justify-between gap-4">
    <div>
      <div class="inline-flex items-center rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold tracking-wide text-emerald-700">
        {{data.showAllLabel}}
      </div>
      <h2 class="mt-3 text-2xl font-bold tracking-tight text-slate-900">✨ {{data.toDo[0].courseName}}</h2>
      <p class="mt-2 text-sm leading-6 text-slate-600">{{data.toDo[0].title}}</p>
    </div>
  </div>

  <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-slate-50">
    <a href="{{data.toDo[0].href}}" aria-label="{{data.toDo[0].titleAriaLabel}}" class="flex items-center justify-between gap-4 p-5 transition hover:bg-slate-100">
      <div class="min-w-0">
        <div class="flex flex-wrap items-center gap-2">
          <span class="rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-800">{{data.toDo[0].pointsText}}</span>
          <span class="text-sm font-medium text-slate-500">{{data.toDo[0].dueText}}</span>
        </div>
        <div class="mt-2 text-lg font-semibold text-slate-900">{{data.toDo[0].courseName}}</div>
        <div class="mt-1 text-sm text-slate-600">{{data.toDo[0].title}}</div>
      </div>
      <div class="shrink-0 rounded-full bg-white px-4 py-2 text-sm font-semibold text-slate-700 shadow-sm ring-1 ring-slate-200">
        {{data.viewGradesLabel}}
      </div>
    </a>
  </div>
</div>